import { Injectable } from '@angular/core';import { HttpClient } from '@angular/common/http';import { Observable } from 'rxjs';import { environment } from '../../environments/environment';import { TodoInterface } from '../types/todo.interface';@Injectable()export class TodoService {  constructor(private http: HttpClient) {}  public getTodos(): Observable<TodoInterface[]> {    const url = `${environment.apiUrl}?userId=1`;    return this.http.get<TodoInterface[]>(url)  }  public deleteById(id: number): Observable<TodoInterface[]> {    const url = `${environment.apiUrl}${id}`;    return this.http.delete<TodoInterface[]>(url)  }  public createTodo(todo: TodoInterface): Observable<TodoInterface[]> {    const url = `${environment.apiUrl}`;    return this.http.post<TodoInterface[]>(url, {todo})  }}